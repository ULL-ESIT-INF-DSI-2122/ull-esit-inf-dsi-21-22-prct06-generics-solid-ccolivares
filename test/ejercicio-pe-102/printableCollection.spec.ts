import 'mocha';
import { expect } from 'chai';
import { PrintableCollection } from '../../src/ejercicio-pe-102/printableColection'



describe ('Prueba clase Printable Coll', () => {
  it('Prueba base', () => {
    expect(1).to.be.eql(1);
  });

});