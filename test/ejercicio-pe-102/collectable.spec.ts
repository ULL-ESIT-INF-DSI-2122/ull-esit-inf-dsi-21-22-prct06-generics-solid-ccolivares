import 'mocha';
import { expect } from 'chai';
import { Collectable } from '../../src/ejercicio-pe-102/collectable'


describe ('Prueba', () => {
  it('Prueba base', () => {
    expect(1).to.be.eql(1);
  });

});